#include<stdio.h>
int main(){
    char name[] = {"John"};
    scanf("%s",&name);// only takes the first word before space bar
    printf("%s",name);
    char wonders[14];
    gets(name);//takes the input full string 
    printf("%s ",name);
    char place[] = "lil baghdad";
    int i;
    for( i = 0 ; place[i]!='\0';i++){

    }
    printf("\n%d\n",i);

}